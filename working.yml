---
- name: Sync Cathexis User Database
  hosts: other_servers
  become: false
  vars:
    db_path: "/usr/nvr/unitconfig/db/users.db"

  tasks:
    - name: Ensure sqlite3 is installed
      apt:
        name: sqlite3
        state: present

    - name: Check if Users and Password tables exist
      command: sqlite3 "{{ db_path }}" "SELECT name FROM sqlite_master WHERE type='table' AND (name='users' OR name='passwords');"
      register: db_checksqlite3
      changed_when: false
      failed_when: false

    - name: Copy data if doesn't exist
      when: "'users' not in db_checksqlite3.stdout"
      synchronize:
        src: "{{ db_path }}"
        dest: "{{ db_path }}"
        mode: push
      delegate_to: CARA-MAIN 

- name: Copy any dependancies
  hosts: other_servers
  become: false
  tasks:
    - name: Rsync users.db from main server
      command: rsync -avz cctvadmin@172.17.4.73:/usr/nvr/unitconfig/db/users.db /usr/nvr/unitconfig/db/users_master.db
      changed_when: false

    - name: Copy multiple scripts to remote machine
      copy:
        src: "{{ item }}"
        dest: "/home/ubuntu/nvr/{{ item | basename }}"
        mode: '0777'
      with_items:
        - /home/ubuntu/userdb/sync.py
        - /home/ubuntu/userdb/password.py

- name: Sync users from gittest to server1 and server2
  hosts: other_servers
  become: true
  vars:
    sync_path: "/home/ubuntu/nvr/sync.py"
    local_db: "/home/ubuntu/nvr/users.db"
    master_db: "/home/ubuntu/nvr/users_master.db"
  tasks:
    - name: Execute script to compare local vs master database
      command: python3 {{ sync_path }} "{{ local_db }}" "{{ master_db }}"
      register: script_output

    - name: script output  
      debug:
        var: script_output

- name: Password sync serv 1
  hosts: main
  become: false
  vars:
    sh_path: "/home/ubuntu/userdb/password.py"
    local_db: "/home/ubuntu/nvr/users.db"
    sev_1: "/home/ubuntu/nvr/users_sev1.db"
    sev_2: "/home/ubuntu/nvr/users_sev2.db"
  tasks:

    - name: Rsync passwords from sev1
      command: rsync -avz ubuntu@172.31.23.120:/home/ubuntu/nvr/users.db /home/ubuntu/nvr/users_sev1.db

    - name: Execute script to compare local vs master database
      command: python3 {{ sh_path }} "{{ local_db }}" "{{ sev_1 }}"
      register: password_output
    
    - name: password output  
      debug:
        var: password_output 

    # - name: Rsync passwords from sev2
    #   command: rsync -avz ubuntu@172.31.31.144:/home/ubuntu/nvr/users.db /home/ubuntu/nvr/users_sev2.db

    # - name: Execute script to compare local vs master database
    #   command: python3 {{ sh_path }} "{{ local_db }}" "{{ sev_2 }}"
    #   register: password_output2

    # - name: password output  
    #   debug:
    #     var: password_output2 
